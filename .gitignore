# Local .terraform directories
**/.terraform/*

# .tfstate files
*.tfstate
*.tfstate.*

# Crash log files
crash.log
crash.*.log

# Ignore Terraform lock files
# These files are generated by Terraform to lock provider versions for consistent dependency resolution.
# Since this repository is for a module and not a root module deployment, these files are unnecessary here.
.terraform.lock.hcl

# Exclude all .tfvars files, which are likely to contain sensitive data, such as
# password, private keys, and other secrets. These should not be part of version
# control as they are data points which are potentially sensitive and subject
# to change depending on the environment.
*.tfvars
*.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Ignore any local .tf files not part of the module, typically used for local development purposes,
# and should not be checked into version control.
*_local.tf
*_local.tf.json

# Include local files you do wish to add to version control using negated pattern
#!example_local.tf

# Ignore transient lock info files created by terraform apply
.terraform.tfstate.lock.info

# Include override files you do wish to add to version control using negated pattern
!tests_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# Ignore direnv environment file, typically used to set local development-specific environment variables
.envrc
